start:
  - run: |
      ./.backend/start.sh
stop:
  - run: |
      ./.backend/stop.sh

auto_start: false

# env:
#   - TABLE_NAME=example

stdout_log: debug
stderr_log: warn

http_proxy:
  prefix: /
  port: 8888
  strip_prefix: /web/apps/neo-jupyter/
